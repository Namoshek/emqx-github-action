name: 'EMQ X MQTT Broker in GitHub Actions'
description: 'Start an EMQ X message broker (on default port 1883 & 8083 for WebSockets)'

branding:
  icon: 'message-square'
  color: 'red'

inputs:
  version:
    description: 'EMQ X version to use'
    required: false
    default: 'latest'
  port:
    description: 'Default port (TCP without SSL)'
    required: false
    default: '1883'
  port-websockets:
    description: 'Port for WebSockets (without SSL)'
    required: false
    default: '8083'

runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.version }}
    - ${{ inputs.port }}
    - ${{ inputs.port-websockets }}
